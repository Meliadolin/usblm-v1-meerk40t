# Build artifacts (regenerated by tools/build_release.py)
setup/
dist/
build/
tools/.cache/
*.spec.bak

# Offline wheelhouse + embedded python (assembled at build time)
wheelhouse/
python-embed.zip
get-pip.py

# Third-party binaries not distributed from the repo
zadig.exe

# Proprietary vendor software (EZCAD, JCZ/Senselock drivers, win7 dumps)
drivers/
ezcad/

# Captures and reverse-engineering artifacts (too big / not distributable)
*.pcap
data/fw_capture*.pcap
data/fw_markrun.pcap
data/fw_upload.pcap

# Working notes (session logs, scratch)
docs/session-ses_*.md
docs/TASK.txt
docs/RESEARCH_V1_ECOSYSTEM.md
TASK.txt

# Raw machine dumps (win7 driver/registry research scratch - kept on
# disk, not published)
docs/reference/

# Logs
*.log
logs/

# Debug output dumps (scratch)
*_out.txt
big_raster.txt

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# OS
Thumbs.db
.DS_Store
